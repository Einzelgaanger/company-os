/**
 * PortalLayout — transfer-guide file map alias for AppLayout (§22).
 * Same shell: ambient backdrop, forest sidebar, mobile chrome, content-canvas.
 */
export { AppLayout as PortalLayout, AppLayout } from "./AppLayout";
